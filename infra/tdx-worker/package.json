{
  "name": "cola-go-tdx-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "devDependencies": {
    "vitest": "4.1.11",
    "wrangler": "4.136.1"
  }
}
