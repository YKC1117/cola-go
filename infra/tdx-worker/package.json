{
  "name": "cola-go-tdx-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "wrangler": "^4.44.0"
  }
}
